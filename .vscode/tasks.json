{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Open Debug Console",
            "type": "shell",
            "command": "${command:workbench.debug.action.focusRepl}",
        }
    ]
}